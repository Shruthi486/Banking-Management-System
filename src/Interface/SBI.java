package Interface;
//Using interface before java 1.8
public interface SBI {
      void deposite(double amount);
      void withdrawl(double amount);
      double checkBalance();
      void accountDetails();
}
